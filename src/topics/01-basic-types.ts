// si no va a cambiar, es const
const name: string = 'Isa';

let hpPoints : number | string = 80;

hpPoints = 'FULL';

let hpPoints2 : number | 'FULL' = 80;

hpPoints2 = 'FULL';

const isAlive : boolean = true;

//console.log(name, hpPoints, hpPoints2, isAlive)

export {}